{
  "name": "@agent-web-portal/auth",
  "version": "0.1.0",
  "description": "Authentication middleware for Agent Web Portal - supports OAuth 2.1, HMAC, and API Key",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "check": "tsc --noEmit && biome check ."
  },
  "dependencies": {},
  "devDependencies": {
    "@types/bun": "latest",
    "bun-types": "^1.3.6"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
