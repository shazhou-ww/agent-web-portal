AWSTemplateFormatVersion: '2010-09-09'
Transform: AWS::Serverless-2016-10-31
Description: Image Workshop - Full Stack AI Image Generation and Editing Service

Globals:
  Function:
    Timeout: 60
    MemorySize: 1024
    Runtime: nodejs20.x
    Architectures:
      - x86_64

Parameters:
  CognitoDomain:
    Type: String
    Description: Cognito hosted UI domain prefix (must be globally unique). Required for external IdP sign-in.
    Default: ""
  GoogleClientId:
    Type: String
    Description: Google OAuth 2.0 Client ID (for Sign in with Google)
    Default: ""
  GoogleClientSecret:
    Type: String
    NoEcho: true
    Description: Google OAuth 2.0 Client Secret (for Sign in with Google)
    Default: ""
  MicrosoftClientId:
    Type: String
    Description: Microsoft (Azure AD) Application (client) ID
    Default: ""
  MicrosoftClientSecret:
    Type: String
    NoEcho: true
    Description: Microsoft (Azure AD) Client Secret
    Default: ""
  CallbackBaseUrl:
    Type: String
    Description: Production UI base URL for Cognito callback/logout (e.g. https://xxx.cloudfront.net). Leave empty on first deploy, then re-deploy with CloudFront URL from outputs.
    Default: ""
  CustomDomainName:
    Type: String
    Description: Custom domain name for CloudFront (e.g. image-workshop.example.com). Requires CustomDomainCertificateArn.
    Default: ""
  CustomDomainCertificateArn:
    Type: String
    Description: ACM certificate ARN for custom domain (must be in us-east-1).
    Default: ""
  CasEndpoint:
    Type: String
    Description: CAS Stack API endpoint URL
    Default: ""
  StabilityApiKeyArn:
    Type: String
    Description: ARN of Secrets Manager secret containing Stability AI API key
    Default: ""
  BflApiKeyArn:
    Type: String
    Description: ARN of Secrets Manager secret containing Black Forest Labs API key
    Default: ""
  VectorizerApiKeyArn:
    Type: String
    Description: ARN of Secrets Manager secret containing Vectorizer.AI credentials (JSON with apiId and apiSecret)
    Default: ""

Resources:
  # ============================================================================
  # Cognito User Pool
  # ============================================================================
  UserPool:
    Type: AWS::Cognito::UserPool
    Properties:
      UserPoolName: !Sub "${AWS::StackName}-users"
      AutoVerifiedAttributes:
        - email
      UsernameAttributes:
        - email
      Policies:
        PasswordPolicy:
          MinimumLength: 8
          RequireLowercase: true
          RequireNumbers: true
          RequireSymbols: false
          RequireUppercase: true
      Schema:
        - Name: email
          AttributeDataType: String
          Mutable: true
          Required: true
        - Name: name
          AttributeDataType: String
          Mutable: true
          Required: false

  UserPoolGoogleIdP:
    Type: AWS::Cognito::UserPoolIdentityProvider
    Condition: HasGoogleIdP
    Properties:
      UserPoolId: !Ref UserPool
      ProviderName: Google
      ProviderType: Google
      ProviderDetails:
        client_id: !Ref GoogleClientId
        client_secret: !Ref GoogleClientSecret
        authorize_scopes: "openid email profile"
      AttributeMapping:
        email: email
        name: name

  UserPoolMicrosoftIdP:
    Type: AWS::Cognito::UserPoolIdentityProvider
    Condition: HasMicrosoftIdP
    Properties:
      UserPoolId: !Ref UserPool
      ProviderName: Microsoft
      ProviderType: OIDC
      ProviderDetails:
        client_id: !Ref MicrosoftClientId
        client_secret: !Ref MicrosoftClientSecret
        authorize_scopes: "openid email profile"
        oidc_issuer: "https://login.microsoftonline.com/9188040d-6c67-4c5b-b112-36a304b66dad/v2.0"
        attributes_request_method: "GET"
      AttributeMapping:
        email: email
        name: name
        username: sub

  # UserPoolClient: 4 variants based on which IdPs are configured
  UserPoolClientNoIdP:
    Type: AWS::Cognito::UserPoolClient
    Condition: NoIdP
    DependsOn: UserPool
    Properties:
      ClientName: !Sub "${AWS::StackName}-client"
      UserPoolId: !Ref UserPool
      GenerateSecret: false
      ExplicitAuthFlows:
        - ALLOW_REFRESH_TOKEN_AUTH
        - ALLOW_USER_SRP_AUTH
      SupportedIdentityProviders:
        - COGNITO
      CallbackURLs: !If
        - HasCallbackBaseUrl
        - - http://localhost:5174/auth/callback
          - http://localhost:3600/auth/callback
          - !Sub
            - "${Base}/auth/callback"
            - Base: !Ref CallbackBaseUrl
        - - http://localhost:5174/auth/callback
          - http://localhost:3600/auth/callback
      LogoutURLs: !If
        - HasCallbackBaseUrl
        - - http://localhost:5174
          - http://localhost:3600
          - !Ref CallbackBaseUrl
        - - http://localhost:5174
          - http://localhost:3600
      AllowedOAuthFlows:
        - code
      AllowedOAuthScopes:
        - openid
        - email
        - profile
      AllowedOAuthFlowsUserPoolClient: true

  UserPoolClientOnlyGoogle:
    Type: AWS::Cognito::UserPoolClient
    Condition: OnlyGoogleIdP
    DependsOn: UserPoolGoogleIdP
    Properties:
      ClientName: !Sub "${AWS::StackName}-client"
      UserPoolId: !Ref UserPool
      GenerateSecret: false
      ExplicitAuthFlows:
        - ALLOW_REFRESH_TOKEN_AUTH
        - ALLOW_USER_SRP_AUTH
      SupportedIdentityProviders:
        - COGNITO
        - Google
      CallbackURLs: !If
        - HasCallbackBaseUrl
        - - http://localhost:5174/auth/callback
          - http://localhost:3600/auth/callback
          - !Sub
            - "${Base}/auth/callback"
            - Base: !Ref CallbackBaseUrl
        - - http://localhost:5174/auth/callback
          - http://localhost:3600/auth/callback
      LogoutURLs: !If
        - HasCallbackBaseUrl
        - - http://localhost:5174
          - http://localhost:3600
          - !Ref CallbackBaseUrl
        - - http://localhost:5174
          - http://localhost:3600
      AllowedOAuthFlows:
        - code
      AllowedOAuthScopes:
        - openid
        - email
        - profile
      AllowedOAuthFlowsUserPoolClient: true

  UserPoolClientOnlyMicrosoft:
    Type: AWS::Cognito::UserPoolClient
    Condition: OnlyMicrosoftIdP
    DependsOn: UserPoolMicrosoftIdP
    Properties:
      ClientName: !Sub "${AWS::StackName}-client"
      UserPoolId: !Ref UserPool
      GenerateSecret: false
      ExplicitAuthFlows:
        - ALLOW_REFRESH_TOKEN_AUTH
        - ALLOW_USER_SRP_AUTH
      SupportedIdentityProviders:
        - COGNITO
        - Microsoft
      CallbackURLs: !If
        - HasCallbackBaseUrl
        - - http://localhost:5174/auth/callback
          - http://localhost:3600/auth/callback
          - !Sub
            - "${Base}/auth/callback"
            - Base: !Ref CallbackBaseUrl
        - - http://localhost:5174/auth/callback
          - http://localhost:3600/auth/callback
      LogoutURLs: !If
        - HasCallbackBaseUrl
        - - http://localhost:5174
          - http://localhost:3600
          - !Ref CallbackBaseUrl
        - - http://localhost:5174
          - http://localhost:3600
      AllowedOAuthFlows:
        - code
      AllowedOAuthScopes:
        - openid
        - email
        - profile
      AllowedOAuthFlowsUserPoolClient: true

  UserPoolClientBothIdP:
    Type: AWS::Cognito::UserPoolClient
    Condition: BothIdP
    DependsOn:
      - UserPoolGoogleIdP
      - UserPoolMicrosoftIdP
    Properties:
      ClientName: !Sub "${AWS::StackName}-client"
      UserPoolId: !Ref UserPool
      GenerateSecret: false
      ExplicitAuthFlows:
        - ALLOW_REFRESH_TOKEN_AUTH
        - ALLOW_USER_SRP_AUTH
      SupportedIdentityProviders:
        - COGNITO
        - Google
        - Microsoft
      CallbackURLs: !If
        - HasCallbackBaseUrl
        - - http://localhost:5174/auth/callback
          - http://localhost:3600/auth/callback
          - !Sub
            - "${Base}/auth/callback"
            - Base: !Ref CallbackBaseUrl
        - - http://localhost:5174/auth/callback
          - http://localhost:3600/auth/callback
      LogoutURLs: !If
        - HasCallbackBaseUrl
        - - http://localhost:5174
          - http://localhost:3600
          - !Ref CallbackBaseUrl
        - - http://localhost:5174
          - http://localhost:3600
      AllowedOAuthFlows:
        - code
      AllowedOAuthScopes:
        - openid
        - email
        - profile
      AllowedOAuthFlowsUserPoolClient: true

  UserPoolDomain:
    Type: AWS::Cognito::UserPoolDomain
    Condition: HasCognitoDomain
    Properties:
      Domain: !Ref CognitoDomain
      UserPoolId: !Ref UserPool

  # ============================================================================
  # DynamoDB Tables
  # ============================================================================
  
  TokensTable:
    Type: AWS::DynamoDB::Table
    Properties:
      TableName: !Sub "${AWS::StackName}-tokens"
      AttributeDefinitions:
        - AttributeName: pk
          AttributeType: S
        - AttributeName: userId
          AttributeType: S
        - AttributeName: createdAt
          AttributeType: N
      KeySchema:
        - AttributeName: pk
          KeyType: HASH
      BillingMode: PAY_PER_REQUEST
      TimeToLiveSpecification:
        AttributeName: expiresAt
        Enabled: true
      GlobalSecondaryIndexes:
        - IndexName: by-user
          KeySchema:
            - AttributeName: userId
              KeyType: HASH
            - AttributeName: createdAt
              KeyType: RANGE
          Projection:
            ProjectionType: ALL

  # ============================================================================
  # S3 Bucket for WebUI Static Files
  # ============================================================================
  UiBucket:
    Type: AWS::S3::Bucket
    Properties:
      BucketName: !Sub "${AWS::StackName}-ui-${AWS::AccountId}"
      PublicAccessBlockConfiguration:
        BlockPublicAcls: true
        BlockPublicPolicy: true
        IgnorePublicAcls: true
        RestrictPublicBuckets: true

  UiBucketPolicy:
    Type: AWS::S3::BucketPolicy
    Properties:
      Bucket: !Ref UiBucket
      PolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Sid: AllowCloudFrontAccess
            Effect: Allow
            Principal:
              Service: cloudfront.amazonaws.com
            Action: s3:GetObject
            Resource: !Sub "${UiBucket.Arn}/*"
            Condition:
              StringEquals:
                AWS:SourceArn: !Sub "arn:aws:cloudfront::${AWS::AccountId}:distribution/${CloudFrontDistribution}"

  # ============================================================================
  # CloudFront Distribution
  # ============================================================================
  CloudFrontDistribution:
    Type: AWS::CloudFront::Distribution
    Properties:
      DistributionConfig:
        Enabled: true
        DefaultRootObject: index.html
        Aliases: !If
          - HasCustomDomain
          - - !Ref CustomDomainName
          - !Ref AWS::NoValue
        ViewerCertificate: !If
          - HasCustomDomain
          - AcmCertificateArn: !Ref CustomDomainCertificateArn
            SslSupportMethod: sni-only
            MinimumProtocolVersion: TLSv1.2_2021
          - CloudFrontDefaultCertificate: true
        Origins:
          - Id: S3Origin
            DomainName: !GetAtt UiBucket.RegionalDomainName
            OriginAccessControlId: !Ref CloudFrontOAC
            S3OriginConfig:
              OriginAccessIdentity: ""
          - Id: ApiOrigin
            DomainName: !Sub "${ImageWorkshopApi}.execute-api.${AWS::Region}.amazonaws.com"
            OriginPath: /prod
            CustomOriginConfig:
              HTTPSPort: 443
              OriginProtocolPolicy: https-only
        DefaultCacheBehavior:
          TargetOriginId: S3Origin
          ViewerProtocolPolicy: redirect-to-https
          AllowedMethods: [GET, HEAD, OPTIONS]
          CachedMethods: [GET, HEAD]
          Compress: true
          CachePolicyId: 658327ea-f89d-4fab-a63d-7e88639e58f6  # CachingOptimized
        CacheBehaviors:
          # Route /api/* to API Gateway
          - PathPattern: /api/*
            TargetOriginId: ApiOrigin
            ViewerProtocolPolicy: https-only
            AllowedMethods: [GET, HEAD, OPTIONS, PUT, POST, PATCH, DELETE]
            CachedMethods: [GET, HEAD]
            CachePolicyId: 4135ea2d-6df8-44a3-9df3-4b5a84be39ad  # CachingDisabled
            OriginRequestPolicyId: b689b0a8-53d0-40ab-baf2-68738e2966ac  # AllViewerExceptHostHeader
            ResponseHeadersPolicyId: !Ref CorsResponseHeadersPolicy
          # Also route /api (without trailing slash) to API Gateway
          - PathPattern: /api
            TargetOriginId: ApiOrigin
            ViewerProtocolPolicy: https-only
            AllowedMethods: [GET, HEAD, OPTIONS, PUT, POST, PATCH, DELETE]
            CachedMethods: [GET, HEAD]
            CachePolicyId: 4135ea2d-6df8-44a3-9df3-4b5a84be39ad  # CachingDisabled
            OriginRequestPolicyId: b689b0a8-53d0-40ab-baf2-68738e2966ac  # AllViewerExceptHostHeader
            ResponseHeadersPolicyId: !Ref CorsResponseHeadersPolicy
        CustomErrorResponses:
          - ErrorCode: 403
            ResponseCode: 200
            ResponsePagePath: /index.html
          - ErrorCode: 404
            ResponseCode: 200
            ResponsePagePath: /index.html
        PriceClass: PriceClass_100

  CorsResponseHeadersPolicy:
    Type: AWS::CloudFront::ResponseHeadersPolicy
    Properties:
      ResponseHeadersPolicyConfig:
        Name: !Sub "${AWS::StackName}-cors-policy"
        CorsConfig:
          AccessControlAllowCredentials: false
          AccessControlAllowHeaders:
            Items:
              - Content-Type
              - Authorization
              - X-AWP-Pubkey
              - X-AWP-Timestamp
              - X-AWP-Signature
          AccessControlAllowMethods:
            Items:
              - GET
              - POST
              - PUT
              - DELETE
              - OPTIONS
          AccessControlAllowOrigins:
            Items:
              - "*"
          AccessControlMaxAgeSec: 86400
          OriginOverride: true

  CloudFrontOAC:
    Type: AWS::CloudFront::OriginAccessControl
    Properties:
      OriginAccessControlConfig:
        Name: !Sub "${AWS::StackName}-oac"
        OriginAccessControlOriginType: s3
        SigningBehavior: always
        SigningProtocol: sigv4

  # ============================================================================
  # Lambda Function
  # ============================================================================
  ImageWorkshopFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub "${AWS::StackName}-handler"
      CodeUri: backend/dist/
      Handler: handler.handler
      Environment:
        Variables:
          TOKENS_TABLE: !Ref TokensTable
          COGNITO_USER_POOL_ID: !Ref UserPool
          COGNITO_CLIENT_ID: !If
            - BothIdP
            - !Ref UserPoolClientBothIdP
            - !If
              - OnlyGoogleIdP
              - !Ref UserPoolClientOnlyGoogle
              - !If
                - OnlyMicrosoftIdP
                - !Ref UserPoolClientOnlyMicrosoft
                - !Ref UserPoolClientNoIdP
          COGNITO_REGION: !Ref AWS::Region
          COGNITO_HOSTED_UI_URL: !If
            - HasCognitoDomain
            - !Sub
              - "https://${Prefix}.auth.${Region}.amazoncognito.com"
              - { Prefix: !Ref CognitoDomain, Region: !Ref AWS::Region }
            - ""
          CAS_ENDPOINT: !Ref CasEndpoint
          CALLBACK_BASE_URL: !Ref CallbackBaseUrl
          STABILITY_API_KEY_ARN: !Ref StabilityApiKeyArn
          BFL_API_KEY_ARN: !Ref BflApiKeyArn
          VECTORIZER_API_KEY_ARN: !Ref VectorizerApiKeyArn
          NODE_OPTIONS: "--enable-source-maps"
      Policies:
        - DynamoDBCrudPolicy:
            TableName: !Ref TokensTable
        - Version: "2012-10-17"
          Statement:
            - Effect: Allow
              Action:
                - cognito-idp:ListUsers
              Resource: !GetAtt UserPool.Arn
        # Secrets Manager access for API keys (awp-examples namespace)
        - Version: "2012-10-17"
          Statement:
            - Effect: Allow
              Action:
                - secretsmanager:GetSecretValue
              Resource:
                - !Sub "arn:aws:secretsmanager:${AWS::Region}:${AWS::AccountId}:secret:awp-examples/*"
      Events:
        ProxyRoute:
          Type: Api
          Properties:
            Path: /{proxy+}
            Method: ANY
            RestApiId: !Ref ImageWorkshopApi
        ProxyOptionsRoute:
          Type: Api
          Properties:
            Path: /{proxy+}
            Method: OPTIONS
            RestApiId: !Ref ImageWorkshopApi
        RootRoute:
          Type: Api
          Properties:
            Path: /
            Method: ANY
            RestApiId: !Ref ImageWorkshopApi
        RootOptionsRoute:
          Type: Api
          Properties:
            Path: /
            Method: OPTIONS
            RestApiId: !Ref ImageWorkshopApi

  # ============================================================================
  # API Gateway
  # ============================================================================
  ImageWorkshopApi:
    Type: AWS::Serverless::Api
    Properties:
      Name: !Sub "${AWS::StackName}-api"
      StageName: prod
      BinaryMediaTypes:
        - "application/octet-stream"
        - "multipart/form-data"
        - "*/*"

Conditions:
  HasCognitoDomain: !Not [!Equals [!Ref CognitoDomain, ""]]
  HasGoogleIdP: !And [!Not [!Equals [!Ref GoogleClientId, ""]], !Not [!Equals [!Ref GoogleClientSecret, ""]]]
  HasMicrosoftIdP: !And [!Not [!Equals [!Ref MicrosoftClientId, ""]], !Not [!Equals [!Ref MicrosoftClientSecret, ""]]]
  HasCallbackBaseUrl: !Not [!Equals [!Ref CallbackBaseUrl, ""]]
  HasCustomDomain: !And [!Not [!Equals [!Ref CustomDomainName, ""]], !Not [!Equals [!Ref CustomDomainCertificateArn, ""]]]
  HasStabilityApiKey: !Not [!Equals [!Ref StabilityApiKeyArn, ""]]
  HasBflApiKey: !Not [!Equals [!Ref BflApiKeyArn, ""]]
  HasAnyApiKey: !Or [!Condition HasStabilityApiKey, !Condition HasBflApiKey]
  NoIdP: !And [!Not [!Condition HasGoogleIdP], !Not [!Condition HasMicrosoftIdP]]
  OnlyGoogleIdP: !And [!Condition HasGoogleIdP, !Not [!Condition HasMicrosoftIdP]]
  OnlyMicrosoftIdP: !And [!Not [!Condition HasGoogleIdP], !Condition HasMicrosoftIdP]
  BothIdP: !And [!Condition HasGoogleIdP, !Condition HasMicrosoftIdP]
  HasAnyIdP: !Or [!Condition HasGoogleIdP, !Condition HasMicrosoftIdP]

Outputs:
  ApiEndpoint:
    Description: API Gateway endpoint URL
    Value: !Sub "https://${ImageWorkshopApi}.execute-api.${AWS::Region}.amazonaws.com/prod"
  
  UserPoolId:
    Description: Cognito User Pool ID
    Value: !Ref UserPool
  
  UserPoolClientId:
    Description: Cognito User Pool Client ID
    Value: !If
      - BothIdP
      - !Ref UserPoolClientBothIdP
      - !If
        - OnlyGoogleIdP
        - !Ref UserPoolClientOnlyGoogle
        - !If
          - OnlyMicrosoftIdP
          - !Ref UserPoolClientOnlyMicrosoft
          - !Ref UserPoolClientNoIdP

  CognitoDomainPrefix:
    Condition: HasCognitoDomain
    Description: Cognito Hosted UI domain prefix
    Value: !Ref CognitoDomain

  CognitoHostedUiUrl:
    Condition: HasCognitoDomain
    Description: Cognito Hosted UI base URL
    Value: !Sub
      - "https://${Prefix}.auth.${Region}.amazoncognito.com"
      - { Prefix: !Ref CognitoDomain, Region: !Ref AWS::Region }

  IdPRedirectUri:
    Condition: HasCognitoDomain
    Description: "Add this URL to Google Cloud Console and/or Azure AD → Authorized redirect URIs"
    Value: !Sub
      - "https://${Prefix}.auth.${Region}.amazoncognito.com/oauth2/idpresponse"
      - { Prefix: !Ref CognitoDomain, Region: !Ref AWS::Region }
  
  TokensTableName:
    Description: DynamoDB table for tokens
    Value: !Ref TokensTable

  UiBucketName:
    Description: S3 bucket for WebUI static files
    Value: !Ref UiBucket

  CloudFrontDistributionId:
    Description: CloudFront Distribution ID
    Value: !Ref CloudFrontDistribution

  CloudFrontUrl:
    Description: CloudFront Distribution URL
    Value: !Sub "https://${CloudFrontDistribution.DomainName}"
