{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "lib": ["ESNext", "DOM"],
    "types": ["node", "bun"],
    "skipLibCheck": true
  },
  "include": ["src", "*.ts"],
  "exclude": ["node_modules", "dist", ".aws-sam"]
}
